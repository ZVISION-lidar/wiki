# 5. 数据输出

## 5.1 坐标系定义

NZ1 直角坐标的定义如下图所示，**I** 为imu坐标系，**L**为点云坐标系， **C**为相机坐标系。

<!-- TODO: 图片资源待补齐（原 assets/img/coordinate.png）-->

<!-- TODO: 图片资源待补齐（原 assets/img/structure2.png）-->

<!-- TODO: 图片资源待补齐（原 assets/img/structure1.png）-->

## 5.2 外参描述

### 外参符号说明

下面使用 $$\mathbf{T}^{\text{A}}_{\text{B}}$$ 来表示坐标转换，具体含义为:

$$
\mathbf{P}_{\text{A}} = \mathbf{T}^{\text{A}}_{\text{B}} \cdot \mathbf{P}_{\text{B}}
$$

其中 $$P_A$$ 和 $$P_B$$ 分别表示物体在A坐标系和B坐标系下坐标。

### Camera - LiDAR

外参 $$\mathbf{T}^{\text{camera}}_{\text{lidar}}$$  不同机器存在差异，请在驱动程序启动后，在config/calib.yaml获取（Tcl_0），具体信息请查看驱动。

### IMU - LiDAR

外参 $$\mathbf{T}^{\text{imu}}_{\text{lidar}}$$  是固定值，数值如下：

$$
\mathbf{T}^{\text{imu}}_{\text{lidar}} =
\begin{bmatrix}
1 & 0 & 0 & -0.02663 \\
0 & 1 & 0 & 0.03447 \\
0 & 0 & 1 & 0.02174 \\
0      & 0      & 0      & 1
\end{bmatrix}
$$

### 其它

- 如果需要 Camera 到 IMU 的外参，可由上述两个外参推导得到。

由 $$\mathbf{P}_{\text{camera}} = \mathbf{T}^{\text{camera}}_{\text{lidar}} \cdot \mathbf{P}_{\text{lidar}}$$ 与 $$\mathbf{P}_{\text{imu}} = \mathbf{T}^{\text{imu}}_{\text{lidar}} \cdot \mathbf{P}_{\text{lidar}}$$，消去 $$\mathbf{P}_{\text{lidar}}$$ 得：

$$
\mathbf{T}^{\text{camera}}_{\text{imu}} = \mathbf{T}^{\text{camera}}_{\text{lidar}} \cdot \left(\mathbf{T}^{\text{imu}}_{\text{lidar}}\right)^{-1}
$$

其中 $$\mathbf{T}^{\text{camera}}_{\text{lidar}}$$ 取自 `config/calib.yaml` 的 `Tcl_0`，$$\mathbf{T}^{\text{imu}}_{\text{lidar}}$$ 为上述固定值。

## 5.3 内参说明

内参我们主要讨论相机的内参，lidar的内参不考虑

### 5.3.1 NZ1 相机内参

- 获取内参的方法：连接设备，运行驱动，会在/ws/src/NZ1_ros_driver/config中生成calib.yaml文件，每个设备略有不同
- calib.yaml内容如下：

```shell
#O1-P010100079
cam_num: 1
img_topic_0: /camera/rgb
Tcl_0: [
-0.00745, -0.99997, -0.00018, 0.03127,
-0.00938, 0.00025, -0.99996, 0.01817,
0.99993, -0.00745, -0.00938, -0.00955,
0, 0, 0, 1
]

cam_0:
  cam_model: FishPoly
  image_width: 1600  # 1920
  image_height: 1296  # 1080
  k2: 5.0379242441551616e-05
  k3: -7.4415257914767799e-03
  k4: -4.1365539417288134e-02
  k5: 5.5346546592564667e-02
  k6: -3.3479736619860797e-02
  k7: 5.9355751390599035e-03
  p1: 0.
  p2: 0.
  A11: 7.3735683773268692e+02
  A12: -4.0977897450998052e-01
  A22: 7.3729158717678535e+02
  u0: 7.9437192080462398e+02
  v0: 6.6625886729029014e+02
  isFast: 0
  numDiff: 3000
  maxIncidentAngle: 120
```

### 5.3.2 相机模型描述

#### NZ1 RGB 相机模型与内参

本文档描述 NZ1 RGB 相机的投影模型及内参参数，供工程参考。

---

#### 1 相机投影模型

NZ1 RGB 使用的是**等距投影（Equidistant）** + **全阶连续多项式畸变模型**。

参考：
- https://github.com/manifoldsdk/NZ_ros_driver/blob/main/include/polynomial_camera.hpp
- https://github.com/gaowenliang/camera_model/blob/master/include/camera_model/camera_models/PolyFisheyeCamera.h

说明：该模型与 OpenCV `cv2.fisheye`（Kannala-Brandt）参数化形式不同，不能直接等价替换。

去畸变后，该模型可简化为标准**针孔模型**。

---

#### 2 多项式鱼眼模型

##### 2.1 投影公式

给定相机坐标系下的三维点：

$$
\mathbf{P_c} = (X, Y, Z)^T
$$

计算入射半径：

$$
r = \sqrt{X^2 + Y^2}
$$

计算入射角：

$$
\theta = \arccos\left(\frac{Z}{\|\mathbf{P_c}\|}\right) = \arccos\left(\frac{Z}{\sqrt{X^2 + Y^2 + Z^2}}\right)
$$

多项式径向映射（注意包含 $\theta$ 线性项）：

$$
\theta_d = \theta + k_2\theta^2 + k_3\theta^3 + k_4\theta^4 + k_5\theta^5 + k_6\theta^6 + k_7\theta^7
$$

投影到畸变平面：

$$
x_d = \frac{\theta_d}{r} \cdot X
$$

$$
y_d = \frac{\theta_d}{r} \cdot Y
$$

---

##### 2.2 仿射变换

$$
\begin{bmatrix}
u \\
v
\end{bmatrix}
=
\begin{bmatrix}
A_{11} & A_{12} \\
0 & A_{22}
\end{bmatrix}
\begin{bmatrix}
x_d \\
y_d
\end{bmatrix}
+
\begin{bmatrix}
u_0 \\
v_0
\end{bmatrix}
$$

##### 参数说明

| 参数 | 说明 |
|------------|-------------|
| k2–k7 | 多项式畸变系数 |
| A11 | X 轴焦距（水平缩放） |
| A12 | 非正交补偿（skew） |
| A22 | Y 轴焦距（垂直缩放） |
| u0, v0 | 主点坐标 |

切向畸变未建模：

$$
p_1 = p_2 = 0
$$

---

### 2.3 去畸变后的针孔模型

去畸变后：

$$
u = f_x \frac{X}{Z} + c_x
$$

$$
v = f_y \frac{Y}{Z} + c_y
$$

内参矩阵：

$$
K =
\begin{bmatrix}
f_x & 0 & c_x \\
0 & f_y & c_y \\
0 & 0 & 1
\end{bmatrix}
$$

去畸变后畸变系数为零。

---

### 2.4 坐标系

NZ1 RGB 遵循 OpenCV 相机坐标系约定：

- X → 右
- Y → 下
- Z → 前

右手坐标系。

---

### 2.5 工程注意事项

- 去畸变不改变外参。
- 高精度重投影请使用多项式鱼眼模型。
- 检测、SLAM、融合等算法框架请使用去畸变后的针孔模型。
- 当 $\theta \to 0$ 时可能出现数值不稳定（$r \to 0$ 导致除零）。

---

### 2.6 总结

NZ1 RGB 相机模型由以下两部分组成：

1. **多项式鱼眼投影**（原始模型）
2. **去畸变针孔表示**（算法兼容模型）

请根据应用场景选择合适的模型。

## 5.4 数据说明

### 5.4.1 点云数据（/NZ1/cloud_raw）

- raw point cloud(NZ1/cloud_raw) has the following fields:

```shell
float32 x             // X axis, in meters
float32 y             // Y axis, in meters
float32 z             // Z axis, in meters
uint8  intensity      // Reflectivity, range 0–255
uint16 confidence     // Point confidence, actual value range from 0 to around 1300 in typical scene, higher value means more reliable. Recommanded filtering threshold is 30-35, should be adjusted accordingly.
float32 offset_time   // Time offset relative to the base timestamp unit: s
```

### 5.4.2 坐标变换数据（/NZ1/tf）

`/NZ1/tf` 发布设备各坐标系之间的变换关系：

| 变换 | 含义 | frame_id | child_frame_id | 频率 |
| :-: | :-: | :-: | :-: | :-: |
| odom → imu | IMU 在 odom 系下的实时位姿 | odom | imu | 10 Hz |
| imu → lidar | IMU-LiDAR 外参 $$\mathbf{T}^{\text{imu}}_{\text{lidar}}$$（将 LiDAR 系坐标变换到 IMU 系，即 $$P_{imu} = \mathbf{T}^{\text{imu}}_{\text{lidar}} \cdot P_{lidar}$$） | imu | lidar | 10 Hz |
| odom → camera | 相机在 odom 系下的实时位姿 | odom | camera | 10 Hz |
| odom → map | 重定位模式下 map 系与 odom 系的关系 | odom | map | < 10 Hz |

说明：

- tf 链为 `odom → imu → lidar`，另有 `odom → camera` 一条分支。IMU 系是设备的本体基准系，接入 ROS 时可直接将其视为 `base_link`，或自行发布一条 `base_link → imu` 的静态变换。
- `odom → imu`、`imu → lidar` 与 `odom → camera` 由参数 `send_odom_baselink_tf` 控制，关闭后不发布。
- 默认按里程计频率（10 Hz）发布；当 `tf_extra_publish_rate` 大于 0 时，驱动会按该频率补发 `odom → imu` 与 `imu → lidar`，并对每帧里程计之后的 200 ms 做突发预发布，避免点云时间戳超前于里程计时 tf2 无法插值。
- `imu → lidar` 携带的是设备标定外参 $$\mathbf{T}^{\text{imu}}_{\text{lidar}}$$（即 `body_T_lidar`，对应 §5.2 固定值矩阵），数值固定不变，以固定频率重复发布，便于订阅端随时获取。
- `odom → camera` 由里程计位姿与两组外参计算得到，相机系遵循 OpenCV 光学约定（X 右、Y 下、Z 前）。
- `odom → map` 仅在**重定位模式**（`custom_map_mode = 2`）下、重定位成功后发布。注意其父子关系为 `odom → map`，与 ROS 常见的 `map → odom` 约定相反，接入导航栈时需自行取逆后重新广播。
- 外参的符号约定与具体数值参见 [5.2 外参描述](#52-外参描述)。

## 5.5 数据输出类型及 ROS Driver 获取链接

- [NZ_ros_driver](https://github.com/manifoldsdk/NZ_ros_driver)
